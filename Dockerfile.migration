FROM python:3.10-slim

# Install dependencies
RUN pip install psycopg2-binary snowflake-connector-python pandas

# Copy migration script
COPY migration_script.py /app/migration_script.py

# Set working directory
WORKDIR /app

# Entry point
CMD ["python", "migration_script.py"]
